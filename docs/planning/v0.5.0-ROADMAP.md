# v0.5.0 Roadmap — "Shareable Scenes"

**Status**: 🚧 Phase 7 in progress on `feature/v0.5.0-phase7-share-codes` (7a + 7b + 7c done — every pattern is on the injected `Random`; 7d–7f remaining)
**Created**: May 10, 2026
**Last updated**: May 10, 2026 (phase 7c batch 5 — Campfire + Fireworks complete; zero `Math.random()` calls remain in `src/patterns/`)
**Focus**: Reproducible scenes — every pattern becomes a seeded function so `splash share <code>` / `splash play <code>` can reconstruct the exact same animation byte-for-byte across machines.

---

## 🎯 Vision

Make ascii-splash **scenes shareable**. Today every pattern silently calls `Math.random()` — restart the same pattern and you get a different rain, a different starfield, a different fish swimming past. v0.5 makes that randomness deterministic and threadable through a u32 seed. The first user-visible feature is share codes; the foundation also unlocks asciinema-style replay, time-of-day automation, and any future feature that needs reproducibility.

---

## ✅ In Scope

- ✅ **Injected `Random` abstraction** — replaces every `Math.random()` site in `src/patterns/` with a constructor-injected PRNG
- ✅ **`splash share`** — print a base32 code for the current `{patternId, presetId, themeId, seed, configHash}`
- ✅ **`splash play <code>`** — decode + run; byte-for-byte replay
- ✅ **In-app `S` key** — copy current state's share code to clipboard
- ✅ **Format versioning** — version-prefix byte so future formats reject gracefully

## ❌ Out of Scope (deferred to v0.6+)

- ❌ asciinema `.cast` export (Phase 8 of the v0.4 roadmap) — separate concern, can ride on top of share codes once they land
- ❌ GIF export (Phase 9 of the v0.4 roadmap)
- ❌ Native graphics protocols (Phase 5 of the v0.4 roadmap — Kitty / iTerm2 / Sixel pass-through)
- ❌ Color-mask sprites (Phase 6 of the v0.4 roadmap)
- ❌ Anything that requires cross-process determinism beyond a single replay (multi-user sync, etc.)

---

## 📊 Starting Point (v0.4.0)

- 23 procedural patterns + optional PhotoPattern + optional LayeredPattern
- 296 `Math.random()` call sites across 22 pattern files (counted at branch start; AnimationEngine, SceneGraph, SpriteManager, TerminalRenderer hold none)
- 7 `Math.random()` sites in `src/engine/CommandExecutor.ts` are **UX random** (`c*`, `c**`, `r` "surprise me") — these stay on `Math.random()`, intentionally non-reproducible
- 2244 tests passing

---

## 🛠️ Design Decisions (locked)

1. **Random is a required constructor arg.** Pattern constructors are `(theme, random, config?)` — theme first, random second, optional config last. Not optional, no Math.random fallback.
2. **One `Random` per pattern instance.** Each pattern's sequence is independent, so a share code can reseed a single pattern without disturbing the rest of the app.
3. **`Mulberry32`** (Tommy Ettinger, public domain) is the default impl. ~10 LOC core, u32 state, period 2^32, statistical quality sufficient for visual use. Implementations are pluggable via the `Random` interface.
4. **`randomSeed()`** bootstrap helper uses `Math.random()` — the one allowed call, isolated to `src/utils/random.ts`, to pick a fresh u32 when the user hasn't supplied a share code.
5. **CommandExecutor stays on `Math.random()`.** Surprise-me commands are intentionally non-deterministic; they're UX, not scene state.
6. **Reference vectors locked.** `tests/unit/utils/random.test.ts` pins the first 5 outputs of `seed=1` so any future PRNG swap has to ship a migration.

---

## 📅 Sub-phase Plan

### Phase 7a — Random foundation + DNA proof of concept ✅

**Commit**: `7f9b4c1`
**Shipped**:

- `src/utils/random.ts` — `Random` interface + `Mulberry32` impl + `randomSeed()` helper
- 21 unit tests, including reference-vector pinning for `seed=1`
- `DNAPattern` fully migrated as proof of concept (2 sites)

### Phase 7b — Shared utilities on injected random ✅

**Commit**: `f18eb65`
**Shipped**:

- `boids.createFlock` threaded with `Random`
- `ParticleSystem` constructor threaded with `Random`
- `AquariumPattern` constructor migrated (boids consumer); 20 internal `Math.random` sites deferred to 7c

### Phase 7c — Pattern migration ✅

**Goal**: every pattern in `src/patterns/` is on the injected `Random`. 296 `Math.random()` sites → 0 in `src/patterns/`.

Landed in 5 batches, simplest first, commits one batch at a time so visual regressions are easy to bisect:

- [x] **Batch 1** (`433dbc9`) — trivial patterns (1–7 sites each): `PlasmaPattern`, `QuicksilverPattern`, `LifePattern`, `MetaballPattern`, `MatrixPattern`, `StarfieldPattern`, `ParticlePattern`, `SpiralPattern`
- [x] **Batch 2** (`df6bebe`) — medium patterns (11–17 sites): `RainPattern`, `TunnelPattern`, `MazePattern`, `SmokePattern`, `LavaLampPattern`
- [x] **Batch 3** (`abdac12`) — finish AquariumPattern (~20 remaining sites; constructor done in 7b)
- [x] **Batch 4** (`123aefa`) — heavy patterns (19–21 sites): `SnowPattern`, `SnowfallParkPattern`, `OceanBeachPattern`, `LightningPattern`, `NightSkyPattern`
- [x] **Batch 5** (`9a32fa2`) — heaviest patterns: `CampfirePattern` (25), `FireworksPattern` (26)

**Verify**:

```bash
grep -rln 'Math.random' src/patterns/   # expect: no output
```

2265 tests passing on the branch.

### Phase 7d — Share-code encoder/decoder

**Tasks**:

- [ ] `src/utils/shareCode.ts` — encode/decode `{patternId, presetId, themeId, seed, configHash}` ↔ base32 (~10 chars)
- [ ] Version-prefix byte (current format = `v1`); future formats reject `v1` codes gracefully
- [ ] `configHash` = stable hash of the non-default config values so two share codes from the same RNG path with different configs decode differently

### Phase 7e — CLI wiring

**Tasks**:

- [ ] `splash share` — print code for the current state, exit
- [ ] `splash play <code>` — decode + boot directly into the encoded state
- [ ] In-app `S` key — copy current state's share code to the system clipboard
- [ ] Friendly error on version-skew or malformed codes

### Phase 7f — Determinism tests

**Tasks**:

- [ ] Round-trip: every supported state encodes + decodes to the same struct
- [ ] Byte-for-byte replay: feed the same share code into two `AnimationEngine` runs, assert frame N matches frame N for several patterns
- [ ] Version-skew rejection: a fabricated `v2` code throws a clear error from the v1 decoder
- [ ] Sanity: `CommandExecutor`'s `c*` / `c**` / `r` paths remain non-deterministic (confirms the UX-random carve-out works as designed)

---

## 🎬 Release Plan

Ship the whole branch as `v0.5.0` once 7c–7f are green. Tag bump uses the existing `npm version minor` pipeline (preversion runs tests + typecheck; postversion pushes main + tag, watches CI, reinstalls global).

---

## 🔭 What Comes After (v0.6+ candidates)

These were sketched in the v0.4 roadmap and remain unstarted; share codes are the foundation they need:

- Phase 5 of v0.4: native graphics protocols (Kitty / iTerm2 / Sixel pass-through)
- Phase 6 of v0.4: color-mask sprites
- Phase 8 of v0.4: asciinema `.cast` export
- Phase 9 of v0.4: GIF export
- Time-of-day pattern automation (trivial atop seeded PRNG + clock)
- Pattern gallery / share-code marketplace UI

---

## 📝 Migration Recipe (for in-batch work)

For each pattern file:

1. Add `import { Random } from '../utils/random.js';`
2. Add `private random: Random;` field
3. Constructor signature `(theme: Theme, random: Random, config?: ...)`
4. In constructor body `this.random = random;`
5. Replace each `Math.random()` site:
   - `Math.random()` → `this.random.next()`
   - `Math.random() * N` → `this.random.next() * N`
   - `min + Math.random() * (max - min)` → `this.random.range(min, max)`
   - `Math.floor(Math.random() * arr.length)` indexing → `this.random.choice(arr)` (throws on empty; guard inline if a test relies on graceful-empty behavior — see `RainPattern.pickChar()` for the canonical pattern)
   - `Math.floor(Math.random() * N) + offset` integer → `this.random.int(offset, offset + N - 1)` (note: `int` is **inclusive** on both ends, unlike `Math.floor(Math.random() * N)` which is `[0, N-1]`)
   - `Math.random() < p` → `this.random.bool(p)`
6. Update `src/main.ts` construction site: pass `new Mulberry32(randomSeed())` as the 2nd arg
7. Update every test file that constructs the pattern to pass `new Mulberry32(42)` (fixed seed for determinism) + add `import { Mulberry32 } from '../../../src/utils/random.js';`

After each batch: `npm run typecheck && npm test`. Both clean before commit. Commit message convention:

```
feat(v0.5.0): phase 7c batch N — <pattern list>
```

Commitlint enforces lowercase subject, header ≤72 chars. Husky lint-staged will run eslint --fix + prettier --write on staged `.ts` files.
